#interger 
#'<-'  --> Assignment operator
x <-2L
typeof(x)

#Keeping L will tell the particular data type the R will consider.
#If there is no L then by default R will consdier it as Double data type
# If we are sure that there is no change in datatype then keep L as like above

#double type
y <- 2.5
typeof(y)

#complex
z<-3+2i
typeof(z)

#Character
a <- "H"
typeof(a)

#Logical or boolean
q1 <- T
typeof(q1)
q2 <- F
typeof(q2)